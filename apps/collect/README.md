# Phase 1a — Collect & Normalize

Web UI input source for Phase 1 discovery. Writes to `docs/discovery/data/`.

```powershell
npm run dev          # http://localhost:3001
npm run expand-corpus # expand to 200+ reviews (batch)
```
