{
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "npx prisma generate && npx prisma db push && npx tsx prisma/seed.ts && next build",
  "framework": "nextjs",
  "env": {
    "DATABASE_URL": "file:./dev.db",
    "N8N_WEBHOOK_SECRET": "blinkit-mvp-webhook-prod"
  }
}
